# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
    include:
    - '*'  
    - refs/tags/*

pool: hosted

steps:
- task: GitHubRelease@2
  inputs:
    gitHubConnection: GitHub
    repositoryName: 'TheLayman/dbms-ass'
    action: 'create'
    target: b887769db196cb8aefddd60dbfee69f827a268fd
    tagSource: 'auto'
    tagPattern: brotherdear[0-9]*